version: 2.1
jobs: 
    build: 
        docker:
            - image: 'circleci/node:10.16.3'
        steps:
            - checkout
            - run: 
                name: Install dependencies
                command: yarn install
            - run:
                name: Run test
                command: yarn test
